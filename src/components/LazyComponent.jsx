import '../styles/index.scss'


const LazyComponent = () => {
  return (
       <section>
        <h3>This is lazy component with text on button click</h3>
      </section>
    )
};

export default LazyComponent;